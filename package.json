{
  "name": "base",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "watch": "yarn build && lerna watch -- lerna run build --since",
    "lint": "lerna run lint",
    "test": "lerna run test",
    "build": "lerna run build",
    "build:ci": "lerna run build --skip-nx-cache",
    "check-all": "yarn lint && yarn test"
  },
  "packageManager": "yarn@3.5.1",
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "dependencies": {
    "lerna": "^7.1.0"
  }
}
